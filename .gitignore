# ---------------------------------------------------------------------------
# 运行期生成的文件：里面有你本机的位置、设置、日志和浏览器缓存，别提交
# ---------------------------------------------------------------------------

# WebView2 的浏览器缓存 / 用户数据（几百 MB，且含本机信息）
.webview2/
.webview2-runtime/

# 日志
pet.log
watcher.log

# 桌宠自己的状态：位置、全部设置（首次启动会自动生成默认值）
pet_position.json
pet_settings.json

# 单实例标记 / 停止请求 / 排查截图
pet.pid
stop.request
selftest.png
黑边修复-用旧版.txt

# Python 缓存
__pycache__/
*.py[cod]
*.egg-info/

# ---------------------------------------------------------------------------
# 本地回滚用的东西（保留了改动前的快照，只在本机有用，不要发布）
# ---------------------------------------------------------------------------
run.py.bak-*
还原-缩放黑边修复.cmd

# ---------------------------------------------------------------------------
# 可选：不想把 Python 依赖（vendor/，约 24MB）一起提交，就把下面这行取消注释，
# 改成用 `pip install -r requirements.txt` 安装（README 里有说明）。
# ---------------------------------------------------------------------------
# vendor/
